{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "Phonebook backend - Full Stack Open Part 3",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/phonebook/dist",
    "deploy:full": "npm run build:ui && git add -A && git commit -m 'rebuild ui' && git push"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "eslint": "^9.9.0",
    "nodemon": "^3.1.4"
  }
}
